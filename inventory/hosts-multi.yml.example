---
# Multi-hypervisor inventory example
#
# This file defines hypervisors and VM connection details.
# VM placement (cluster_nodes) and resource overrides (vm_specs) go in a
# separate extra-vars file -- see lab-vars.yml.example.
#
# Setup:
#   1. cp hosts-multi.yml.example hosts-multi.yml
#   2. cp lab-vars.yml.example lab-vars.yml
#   3. Edit both files with your lab values
#   4. Deploy:
#        make deploy INVENTORY=inventory/hosts-multi.yml CUSTOM_VARS=inventory/lab-vars.yml
#
# Why two files?
#   cluster_nodes and vm_specs are defined in group_vars/all/main.yml.
#   Ansible variable precedence means inventory vars cannot override
#   group_vars. CUSTOM_VARS uses -e @file which has the highest precedence.
#
# Both hosts-*.yml and *-vars.yml are in .gitignore and will NOT be committed.
#
# Requirements:
#   - SSH access (as root) from Ansible controller to each hypervisor
#   - The bridge specified in bridge_bm must exist on each hypervisor
#   - All bridges must share the same L2 domain (same VLAN / broadcast domain)
#   - RHEL golden image must be present on each hypervisor at image_dir
all:
  children:
    hypervisors:
      hosts:
        hypervisor01:
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
          image_dir: /var/lib/libvirt/images
          bridge_bm: br-2003
        hypervisor02:
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
          image_dir: /var/lib/libvirt/images
          bridge_bm: br-2003

    utility_vms:
      hosts:
        utility:
          ansible_host: 192.168.203.254
          ansible_user: root
          ansible_ssh_private_key_file: files/.ssh/id_rsa
          ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"

    ceph_vms:
      hosts:
        ceph:
          ansible_host: 192.168.203.252
          ansible_user: root
          ansible_ssh_private_key_file: files/.ssh/id_rsa
          ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"

